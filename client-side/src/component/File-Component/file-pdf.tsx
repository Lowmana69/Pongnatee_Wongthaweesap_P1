import React from 'react';
import { } from '@react-pdf/renderer';

export default function File() {
  return (
    <Document src={ uri: valid-url, method: 'GET', headers: {}, body: '' }>
      <Page pageNumber={1} />
    </Document>
  );
}